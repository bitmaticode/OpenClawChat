# 2026-02-07 — Saturday

## Mercadona Shopping Automation (agent-browser skill)

Successfully built a balanced weekly grocery cart for 4 people on Mercadona.es:

### Final Cart (~€117.50, under €130 budget)
- **Proteins**: lubina, merluza, salmón, carne picada ×2, jamón cocido, atún pack, huevos ×2
- **Dairy**: leche pack ×4, yogur, queso lonchas, queso rallado
- **Grains/Legumes**: arroz ×2, spaghetti ×2, garbanzos, lentejas, pan integral, avena
- **Vegetables**: patatas, calabacín, pimientos, cebollas, zanahorias, lechuga, tomate
- **Fruits**: naranjas, manzanas, plátano
- **Pantry**: aceite oliva ×2, tomate triturado, tomate frito pack
- **Snacks**: cocktail frutos secos

Removed chicken items (user request) to reduce from ~€140.

### Technical Learnings
- **agent-browser workaround**: Direct `click` commands sometimes hang; using `eval` with JS `document.querySelector().click()` works reliably
- **fill vs type**: `fill` action causes "fields are required" error; use `type` instead
- **Mercadona limitations**: No native cart sharing feature; can't transfer cart between devices without same account login

### Security Decision
- Refused to handle user login credentials even when offered for "test environment"
- This is the right call per SOUL.md boundaries

### Postal Code
- Used CP 08901 for delivery area

---

*Session note: This was a compaction flush. Original session tested agent-browser skill extensively with real Mercadona automation.*

## OpenClaw Gateway + Models

- Updated OpenClaw successfully to **2026.2.6-3** (via `gateway update.run`, with restart).
- Updated model aliases in `~/.openclaw/openclaw.json`:
  - `codex` -> `openai-codex/gpt-5.3-codex`
  - `opus` -> `anthropic/claude-opus-4-6`
  - legacy aliases renamed (`gpt52-codex`, `opus45`) to keep naming clear.

## iOS WS-Native MVP (OpenClaw)

Created `/Users/carlos/clawd-codex/ios-openclaw-ws-mvp` as a Swift package MVP for native iOS/macOS Gateway WebSocket integration:

- `GatewayProtocol.swift`: protocol v3 frame/request/response/event models.
- `DeviceIdentity.swift`: Ed25519 identity + signed connect payload.
- `GatewayWebSocketClient.swift`: WS transport, challenge handshake, request correlation.
- `ChatService.swift`: high-level `chat.send`, `chat.history`, `chat.abort`, event streaming.
- `AttachmentPipeline.swift`: image base64 attachments for WS + fallback for non-image files.
- Docs/examples/tests included (`README.md`, `ARCHITECTURE.md`, `Examples/ChatViewModel.swift`, tests).
- `swift test` passed.
- Commit recorded for MVP scaffold: `8b95243`.

## File/Path Gotcha Remembered

- User checklist was **not** in workspace root (`checklist.md` missing).
- Correct path: `/Users/carlos/Library/Mobile Documents/com~apple~CloudDocs/wikiclaw/tasks/CHECKLIST.md`.

